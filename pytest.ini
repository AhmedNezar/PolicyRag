[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = strict
markers =
    integration: tests that use real infrastructure such as Postgres
    ai_eval: tests that call a live AI model and may incur cost
    e2e: tests that exercise complete API request journeys
